class Ball:
        def __init__(self, position, velocity, color, number):
                self.position = position
                self.velocity = velocity
                self.color = color
                self.responsed = False
                self.living = True
                self.score = 1
                self.number = number
                
